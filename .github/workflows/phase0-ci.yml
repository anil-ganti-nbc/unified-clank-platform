name: Phase 0 CI

on:
  pull_request:
  push:
    branches: [main, diagnostic-clank-2026-08]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phase0-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  NO_COLOR: "1"

jobs:
  quality:
    name: ci / install-lint-types
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
      - name: Clean locked installation
        run: |
          python -m pip install build==1.2.2.post1 pyright==1.1.405
          python -m pip install -e "./clank-runtime[dev]" -e "./clank-fleet[dev]" -e "./clank-desktop[dev]"
      - name: Verify dependency lock
        run: |
          python -c 'import pathlib; assert all((pathlib.Path(p) / "pyproject.toml").is_file() for p in ("clank-runtime", "clank-fleet", "clank-desktop"))'
      - name: Required correctness lint
        run: |
          python -m pip install ruff==0.12.11
          ruff check --select E9,F63,F7,F82 .
      - name: Type or bytecode validation
        run: |
          pyright clank-runtime/src clank-fleet/src clank-desktop/src
  advisory-style:
    name: advisory / full-style-format
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
      - run: python -m pip install ruff==0.12.11
      - run: ruff check . && ruff format --check .
  tests:
    name: ci / tests-and-collection
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
      - name: Clean locked installation
        run: |
          python -m pip install build==1.2.2.post1 pyright==1.1.405
          python -m pip install -e "./clank-runtime[dev]" -e "./clank-fleet[dev]" -e "./clank-desktop[dev]"
      - name: Complete test collection
        shell: bash
        run: |
          set -o pipefail
          for package in clank-runtime clank-fleet clank-desktop; do (cd "$package" && python -m pytest --collect-only -q); done | tee test-collection.txt
          test -s test-collection.txt
      - name: Full hermetic test suite
        env:
          NO_PROXY: "127.0.0.1,localhost,::1"
          HTTP_PROXY: "http://127.0.0.1:9"
          HTTPS_PROXY: "http://127.0.0.1:9"
          ALL_PROXY: "http://127.0.0.1:9"
        run: |
          for package in clank-runtime clank-fleet clank-desktop; do (cd "$package" && python -m pytest -q); done
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: unified-clank-platform-test-collection
          path: test-collection.txt
          if-no-files-found: error
  security:
    name: security / secrets-and-dependencies
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
      - name: Scan Git history and tree for secrets
        shell: bash
        run: |
          go install github.com/zricethezav/gitleaks/v8@v8.30.0
          "$(go env GOPATH)/bin/gitleaks" git --redact --no-banner
          "$(go env GOPATH)/bin/gitleaks" dir . --redact --no-banner
      - name: Install and audit locked dependencies
        run: |
          python -m pip install build==1.2.2.post1 pyright==1.1.405
          python -m pip install -e "./clank-runtime[dev]" -e "./clank-fleet[dev]" -e "./clank-desktop[dev]"
          python -m pip install pip-audit==2.9.0
          pip-audit --progress-spinner off
  build:
    name: build / package-sbom-provenance
    runs-on: ubuntu-latest
    needs: [quality, tests]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
      - name: Build from clean source
        run: |
          python -m pip install build==1.2.2.post1 pyright==1.1.405
          python -m pip install -e "./clank-runtime[dev]" -e "./clank-fleet[dev]" -e "./clank-desktop[dev]"
          python -m pip install build==1.2.2.post1 cyclonedx-bom==7.1.0
          for package in clank-runtime clank-fleet clank-desktop; do python -m build "$package"; done
      - name: Generate reproducible SBOM and source provenance
        shell: bash
        run: |
          mkdir -p dist
          cyclonedx-py environment --output-reproducible --of JSON -o dist/sbom.cdx.json "python"
          python -c 'import hashlib,json,os,pathlib; files=[p for p in pathlib.Path(".").rglob("*lock*") if p.is_file() and ".git" not in p.parts]; data={"git_sha":os.environ["GITHUB_SHA"],"repository":os.environ["GITHUB_REPOSITORY"],"locks":{str(p):hashlib.sha256(p.read_bytes()).hexdigest() for p in files}}; pathlib.Path("dist/provenance.json").write_text(json.dumps(data,sort_keys=True,indent=2)+"\n")'
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: unified-clank-platform-build-sbom-provenance
          path: dist/
          if-no-files-found: error
