# Reproducible builds

This repository is frozen and proposed for supersession; it has no promotable release build. CI clean-installs and builds the three Python packages only to detect drift, then records package artifacts, a CycloneDX SBOM, and exact Git SHA. The digest-pinned Docker templates remain non-production examples. Do not publish or promote any output.
